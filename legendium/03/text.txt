# Установка python-dotenv
pip install python-dotenv

# Установка django-debug-toolbar
pip install django-debug-toolbar

# Запуск в prod среде по умолчанию в файлах manage.py и wsgi.py
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'legendium.settings.prod')

# Разрешить запуск скрипта
chmod +x /path/to/file

# Установка dos2unix
sudo apt install dos2unix

# Запуск dos2unix
dos2unix /path/to/file

# Команды git
git add .
git commit -m "Название"
git push origin main
